// Host loader entry for the browser-only CAD preview UI plugin.
// Provides no host-side behavior; the browser half lives in lib/client.js
// and is delivered by dsh-client-modules via package.json `dsh.client`.
export function apply() {}
